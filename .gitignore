node_modules

# Compiled JS — all ignored except root plugin artifacts and eslint config.
*.js
*.js.map
tsconfig.tsbuildinfo
_dist/
build/
dist/

!MeansTvScript.js
!eslint.config.js

# Sensitive browser captures are local analysis inputs, never plugin artifacts.
*.har
